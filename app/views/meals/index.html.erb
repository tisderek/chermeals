<h2>Up for grabs</h2>

<div class="ui cards">
  <% @user.name %>
  <%= render partial: "index_card", collection: @available_meals, as: :meal, locals: {user: @user} %>
</div>

<div class="ui hidden divider"></div>

<h2>Taken</h2>

<div class="ui cards">
  <%= render partial: "index_card", collection: @taken_meals, as: :meal, locals: {user: @user} %>
</div>
